'use strict';

/**
 * codx-room-template service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::codx-room-template.codx-room-template');
